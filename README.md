# 🎉 Pozdravish Bot — Telegram-бот генерации открыток

Telegram-бот для генерации персонализированных поздравительных открыток с помощью ИИ.  
Бот создаёт уникальный фон через **ProTalk API**, затем накладывает поздравительный текст с выбранным шрифтом через **Pillow**.  
Хостинг — бесплатный **Vercel** (через Webhooks). Состояние пользователей и кредиты хранятся в **Upstash Redis**.

---

## ✨ Возможности

- 🖼 **Генерация уникального фона** через ProTalk AI API по стилизованным промптам
- ✍️ **Наложение персонального текста** по центру открытки с тенью для читаемости
- 🎨 **6 стилей оформления**: Акварель, Масло, Неон, Пастель, Винтаж, Минимализм
- 🔤 **4 шрифта на выбор**: Lobster, Caveat, Pacifico, Comfortaa (с превью)
- 🎁 **5 поводов**: День рождения, Свадьба, Рождение ребёнка, 8 марта, Завершение учёбы
- 💳 **Система кредитов**: 3 бесплатные открытки новому пользователю
- 💰 **Монетизация через ЮКассу**: пакеты на 3 / 5 / 10 открыток

---

## 🗂 Структура проекта

```
├── api/
│   └── index.py              # Основной код бота (FastAPI + aiogram)
├── Lobster-Regular.ttf       # Шрифт Lobster
├── Caveat-Regular.ttf        # Шрифт Caveat
├── Pacifico-Regular.ttf      # Шрифт Pacifico
├── Comfortaa-Regular.ttf     # Шрифт Comfortaa
├── fonts_preview.jpg         # Превью всех шрифтов (показывается пользователю)
├── requirements.txt          # Зависимости Python
├── vercel.json               # Конфигурация деплоя на Vercel
└── README.md
```

---

## 🛠 Технологический стек

| Библиотека        | Версия   | Назначение                          |
|-------------------|----------|-------------------------------------|
| `aiogram`         | 3.13.1   | Telegram Bot Framework              |
| `fastapi`         | 0.110.0  | HTTP-сервер для приёма webhook      |
| `uvicorn`         | 0.27.1   | ASGI-сервер                         |
| `aiohttp`         | 3.9.3    | Async HTTP-клиент для ProTalk API   |
| `Pillow`          | 10.2.0   | Обработка изображений, текст        |
| `upstash-redis`   | 1.1.0    | KV-хранилище кредитов и состояний   |
| `pydantic`        | 2.6.3    | Валидация данных                    |

---

## 🚀 Деплой на Vercel

### 1. Импорт репозитория

1. Войдите в [Vercel](https://vercel.com/) и нажмите **Add New → Project**.
2. Импортируйте этот репозиторий.
3. В разделе **Environment Variables** добавьте все переменные окружения (см. раздел ниже).
4. Нажмите **Deploy** и дождитесь окончания сборки.  
   Вы получите URL вида: `https://ваш-проект.vercel.app`

### 2. Настройка Webhook

Vercel переводит функции в спящий режим при отсутствии запросов, поэтому бот работает через **webhook**, а не через polling.

Откройте в браузере следующую ссылку, подставив ваши значения:

```
https://api.telegram.org/bot<TELEGRAM_BOT_TOKEN>/setWebhook?url=<ВАШ_VERCEL_URL>/api/webhook
```

**Пример:**
```
https://api.telegram.org/bot123456789:ABCdefGHI/setWebhook?url=https://my-bot.vercel.app/api/webhook
```

При успешной настройке браузер вернёт:
```json
{"ok": true, "result": true, "description": "Webhook was set"}
```

### 3. Готово! 🎉

Откройте бота в Telegram и отправьте `/start`.

---

## ⚙️ Переменные окружения

| Переменная                  | Описание                                                       | Обязательная |
|-----------------------------|----------------------------------------------------------------|:---:|
| `TELEGRAM_BOT_TOKEN`        | Токен бота от [@BotFather](https://t.me/BotFather)            | ✅ |
| `PROTALK_TOKEN`             | Токен доступа к ProTalk API                                    | ✅ |
| `PROTALK_BOT_ID`            | ID бота в ProTalk (по умолчанию: `23141`)                      | ✅ |
| `PROTALK_FUNCTION_ID`       | ID функции генерации изображений (по умолчанию: `609`)         | ✅ |
| `YUKASSA_PROVIDER_TOKEN`    | Токен провайдера ЮКасса для приёма платежей                   | ✅ |
| `UPSTASH_REDIS_REST_URL`    | URL Upstash Redis (для хранения кредитов и состояний)          | ✅ |
| `UPSTASH_REDIS_REST_TOKEN`  | Токен доступа к Upstash Redis                                  | ✅ |
| `ADMIN_ID`                  | Telegram ID администратора бота для выполнения скрытых команд | ✅ |

> Upstash Redis можно бесплатно создать на [upstash.com](https://upstash.com/).

---

## 💬 Сценарий работы бота

```
/start
  └─► Выбор повода (кнопки)
        └─► Выбор стиля оформления (кнопки)
              └─► Просмотр превью шрифтов + выбор шрифта (кнопки)
                    └─► Ввод имени получателя (текст)
                          ├─► [есть кредиты] Генерация открытки → отправка фото
                          └─► [нет кредитов] Предложение купить пакет → оплата → генерация
```

### Доступные поводы
- 🎂 День рождения
- 💍 Свадьба
- 👶 Рождение ребёнка
- 🌸 8 марта
- 🎓 Завершение учёбы

### Доступные стили
- **Акварель** — акварельный фон с детализированными элементами по краям
- **Масло** — классическая живопись маслом, богатая текстура
- **Неон** — киберпанк-стиль с неоновыми элементами на тёмном фоне
- **Пастель** — мягкие переходы цвета, эффект сухой пастели
- **Винтаж** — стиль советских почтовых открыток
- **Минимализм** — минимум деталей, максимум пространства

---

## 💳 Система кредитов

- Каждый новый пользователь получает **3 бесплатные открытки**
- При нулевом балансе бот предлагает купить пакет:

| Пакет       | Цена    |
|-------------|---------|
| 3 открытки  | 90 руб. |
| 5 открыток  | 150 руб.|
| 10 открыток | 300 руб.|

- Платежи принимаются через **ЮКассу** (Telegram Payments API)
- Незавершённый запрос сохраняется в Redis и автоматически выполняется после оплаты

---

## 🤖 Команды бота

| Команда    | Описание                              |
|------------|---------------------------------------|
| `/start`   | Начать работу, показать баланс        |
| `/balance` | Показать текущий баланс кредитов      |
| `/reset`   | Сбросить счётчик кредитов (только admin) |
