# ProTalk Telegram Bot на Vercel 🚀

Этот репозиторий содержит код Telegram-бота для генерации открыток через API ProTalk, адаптированный для бесплатного хостинга на Vercel (через Webhooks).

## 🛠 Как запустить проект

### 1. Деплой на Vercel
1. Нажмите кнопку импорта репозитория в вашем кабинете [Vercel](https://vercel.com/).
2. В разделе **Environment Variables** (переменные окружения) добавьте следующие ключи:
   - `TELEGRAM_BOT_TOKEN` — токен вашего бота (получить у [@BotFather](https://t.me/BotFather))
   - `PROTALK_TOKEN` — ваш токен доступа к ProTalk
   - `PROTALK_BOT_ID` — ID вашего бота в ProTalk (по умолчанию `23141`)
   - `PROTALK_FUNCTION_ID` — ID функции генерации в ProTalk (по умолчанию `609`)
3. Нажмите **Deploy** и дождитесь окончания сборки. Вы получите URL-адрес вида `https://ваштестпроект.vercel.app`.

### 2. Настройка Webhook для Telegram
Vercel усыпляет бота, когда им никто не пользуется, поэтому бот работает через **вебхуки** (webhook), а не через постоянный опрос (polling). 

Вам нужно сказать серверам Telegram, чтобы они отправляли сообщения пользователей на ваш Vercel-адрес.

Откройте в браузере следующую ссылку, заменив `<TELEGRAM_BOT_TOKEN>` и `<ВАШ_VERCEL_URL>` на ваши данные:

```text
https://api.telegram.org/bot<TELEGRAM_BOT_TOKEN>/setWebhook?url=<ВАШ_VERCEL_URL>/api/webhook
```

Пример правильной ссылки:
`https://api.telegram.org/bot123456789:ABCdefGHI/setWebhook?url=https://protalk-bot-xyz.vercel.app/api/webhook`

Если всё сделано верно, браузер ответит:
`{"ok":true,"result":true,"description":"Webhook was set"}`

### 3. Готово! 🎉
Откройте вашего бота в Telegram и отправьте команду `/start`!